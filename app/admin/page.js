'use client';import {useEffect,useState} from 'react';import './dashboard.css';
const money=n=>`${Number(n||0).toFixed(2)} KM`;const labels={pending:'Na čekanju',processing:'U obradi',shipped:'Poslano',completed:'Završeno',cancelled:'Otkazano'};
export default function AdminHome(){const [d,setD]=useState(null),[error,setError]=useState('');useEffect(()=>{fetch('/api/admin/dashboard').then(async r=>{if(!r.ok)throw Error((await r.json()).message);return r.json()}).then(setD).catch(e=>setError(e.message))},[]);if(!d)return <div className="dash"><h1>{error||'Učitavanje statistike…'}</h1></div>;const max=Math.max(1,...d.sales.map(x=>Number(x.total)));return <div className="dash"><div className="dashIntro"><div><h1>Pregled poslovanja</h1><p>Dobro došli, {d.user.name}. Podaci se prikazuju iz baze u realnom vremenu.</p></div><div><span>{new Date().toLocaleDateString('bs-BA',{weekday:'long',day:'numeric',month:'long',year:'numeric'})}</span><small>Live podaci</small></div></div><div className="stats"><Stat icon="🛒" label="Narudžbe" value={d.stats.orders}/><Stat icon="▥" label="Plaćeni prihod" value={money(d.stats.revenue)}/><Stat icon="♙" label="Članice" value={d.stats.members}/><Stat icon="◇" label="Aktivni proizvodi" value={d.stats.products}/></div><div className="dashMid"><section className="dashPanel sales"><div className="panelHead"><h2>Prodaja zadnjih 7 dana</h2><p>Plaćeni prihod<strong>{money(d.sales.reduce((s,x)=>s+Number(x.total),0))}</strong></p></div><div className="chart"><div className="axis"><span>{money(max)}</span><span>75%</span><span>50%</span><span>25%</span><span>0</span></div>{d.sales.length?d.sales.map(x=><div className="barCol" key={x.day}><i style={{height:`${Math.max(4,Number(x.total)/max*130)}px`}}/><b>{new Date(x.day).toLocaleDateString('bs-BA',{weekday:'short'})}</b><small>{new Date(x.day).toLocaleDateString('bs-BA',{day:'numeric',month:'short'})}</small></div>):<p>Nema plaćenih narudžbi u ovom periodu.</p>}</div></section><section className="dashPanel quick"><h2>Brze akcije</h2><a className="primary" href="/admin/proizvodi/luna-haljina"><b>＋</b> Uredi proizvod <i>›</i></a><a href="/admin/narudzbe"><b>◇</b> Otvori narudžbe <i>›</i></a><hr/><blockquote>„Male stvari čine veliku razliku.“<small>MADE BY ALY</small></blockquote></section></div><div className="dashBottom"><section className="dashPanel orders"><div className="panelHead"><h2>Posljednje narudžbe</h2><a href="/admin/narudzbe">Pogledaj sve →</a></div><div className="orderTable"><div className="orow ohead"><b>#</b><b>Kupac</b><b>Datum</b><b>Iznos</b><b>Status</b></div>{d.orders.map(o=><div className="orow" key={o.id}><span>{o.order_number}</span><span>{o.customer_name}</span><span>{new Date(o.created_at).toLocaleDateString('bs-BA')}</span><span>{money(o.total)}</span><span><i className={o.status==='completed'?'paid':o.status==='shipped'?'sent':'process'}>{labels[o.status]}</i></span></div>)}</div></section><section className="dashPanel newest"><div className="panelHead"><h2>Nove ALY članice</h2>{d.user.role==='admin'&&<a href="/admin/clanice">Pogledaj sve →</a>}</div>{d.user.role==='admin'?d.members.map(m=><div className="newMember" key={m.id}><b>{m.name?.[0]||'A'}</b><span>{m.name||'Nova članica'}</span><time>{new Date(m.created_at).toLocaleDateString('bs-BA')}</time></div>):<div className="emptyHint">Detalji članica dostupni su administratorima.</div>}</section></div></div>}
function Stat({icon,label,value}){return <section className="dashPanel stat"><i>{icon}</i><div><span>{label}</span><strong>{value}</strong><small>stvarni podaci iz baze</small></div></section>}
