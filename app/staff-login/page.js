'use client';import {useState} from 'react';import './style.css';
export default function StaffLogin(){const [email,setEmail]=useState('admin@madebyaly.com'),[password,setPassword]=useState(''),[error,setError]=useState(''),[busy,setBusy]=useState(false);async function submit(e){e.preventDefault();setBusy(true);setError('');const r=await fetch('/api/admin/auth/login',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({email,password})});const d=await r.json();if(r.ok)location.href='/admin';else setError(d.message||'Prijava nije uspjela.');setBusy(false)}return <main className="staffLogin"><form onSubmit={submit}><div className="staffBrand">MADE BY ALY<small>Administracija</small></div><h1>Prijava za tim</h1><p>Pristup je dozvoljen samo ovlaštenim članovima tima.</p><label>E-mail<input type="email" value={email} onChange={e=>setEmail(e.target.value)} required/></label><label>Lozinka<input type="password" value={password} onChange={e=>setPassword(e.target.value)} required/></label>{error&&<div className="loginError">{error}</div>}<button disabled={busy}>{busy?'Prijava…':'Prijavi se'}</button></form></main>}
