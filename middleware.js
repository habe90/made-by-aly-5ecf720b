import {NextResponse} from 'next/server';
export async function middleware(request){const {pathname}=request.nextUrl;const alwaysAllowed=pathname==='/coming-soon'||pathname==='/staff-login'||pathname.startsWith('/admin')||pathname.startsWith('/_next')||pathname==='/api/site-settings'||pathname==='/api/maintenance-access'||pathname.startsWith('/api/admin/auth');if(alwaysAllowed)return NextResponse.next();try{const url=new URL('/api/maintenance-access',request.url);const r=await fetch(url,{cache:'no-store',headers:{cookie:request.headers.get('cookie')||''}});const access=await r.json();if(access.maintenance&&!access.admin){if(pathname.startsWith('/api/'))return NextResponse.json({message:'Shop je privremeno zatvoren.'},{status:503});return NextResponse.redirect(new URL('/coming-soon',request.url))}}catch{}return NextResponse.next()}
export const config={matcher:['/((?!_next/static|_next/image|favicon.ico|images/).*)']};
